services:
  web:
    image: ${DOCKER_IMAGE_TAG}
    container_name: absinternational-app
    restart: unless-stopped
    ports:
      - "3002:3000"
    env_file:
      - .env.local
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
